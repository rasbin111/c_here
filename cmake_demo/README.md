# Steps

1. Create CMakeLists.txt
2. make build folder & inside build folder run cmake (cmake ..)
3. Build project using make
4. Add code, update code, etc. then jump to step 3.
5. If new .c files or alter dependencies then jump to step 1.
